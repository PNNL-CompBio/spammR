# Helper function for computing pairwise differences between sample values

This function is called in distance_based_analysis()

## Usage

``` r
calc_diff_pairwiseSamples(feature_values)
```

## Arguments

- feature_values:

  A 1 x n vector containing values for a feature for n

## Value

Data frame of differences samples. names(feature_values) should contain
names of samples
